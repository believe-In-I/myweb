# 欢迎使用文档中心

这是一个示例文档，用于展示 Markdown 导航功能。

## 功能特性

- 左侧显示文档列表
- 点击标题动态加载内容
- 支持 Markdown 渲染
- 响应式布局设计

## 使用方法

1. 在 `data` 文件夹中添加 `.md` 文件
2. 文件第一行将自动作为标题显示
3. 点击左侧标题即可查看文档内容

## 代码示例

```javascript
const example = "Hello World";
console.log(example);
```

> 提示：支持丰富的 Markdown 语法
